/* global describe, it*/
import assert from 'assert';
import { getAdi } from '../../src/crashes/adi';

describe('getAdi', () => {
  // if (process.env.CRASH_STATS_TOKEN) {
  //   it('should return an array for Firefox', async () => {
  //     const data = await getAdi({
  //       dateRange: [new Date('2016-01-01'), new Date('2016-01-30')],
  //     });
  //     assert(Array.isArray(data));
  //     assert(data.length > 1);
  //   });
  //
  //   it('should return an array for Fennec', async () => {
  //     const data = await getAdi({
  //       dateRange: [new Date('2016-01-01'), new Date('2016-01-30')],
  //       product: 'fennec',
  //     });
  //     assert(Array.isArray(data));
  //     assert(data.length > 1);
  //   });
  // }
});
